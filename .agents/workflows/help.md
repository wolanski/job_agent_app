---
description: help — Workflow index
---

# /help — Workflow index

Available workflows (Lean):

**Phase A — Collaborative Design:**
- /explore : investigatory or exploratory work (spikes, research, audits)
- /advise  : get structured recommendations on a decision
- /seed    : validate workspace + confirm `make check` gate
- /plan    : build-ready context + Stories planning

**Phase B — Disciplined Build:**
- /next    : execute next task (JIT tasks + tests + evidence)
- /check   : run `make check` validation gate
- /release : hardening + release candidate + notes

**Any phase:**
- /archive : continuously clean up PROGRESS.md by moving old logs to an archive
- /ccr     : raise Contract Change Request (STOP)
- /help    : this index